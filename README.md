To use, define include on a host and define a variable in the format:

```
trac:
  instances:
    <instance nam>:
      path: "<path for trac instance"
      config:
        <section heading>:
          <option>: <value>
```
